{
  "name": "cv-generator",
  "version": "1.0.0",
  "description": "AI-powered CV Generator with React frontend and Node.js backend",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "cv",
    "generator",
    "ai",
    "react",
    "nodejs"
  ],
  "author": "CV Generator Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
